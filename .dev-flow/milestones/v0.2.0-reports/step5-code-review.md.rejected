# 代码审查：v0.2.0 Phase 1 整合版

## 变更概览

- **变更文件**：13 个（8 个源文件 + 新增 `src/util.rs` + 文档/配置）
- **审查维度**：正确性、安全性、健壮性、可维护性、性能、规范（AGENTS.md 无代码审查维度段落，使用内置默认集）
- **审查时间**：2025-08-31

## 审查维度逐项检查

### 1. 正确性 ✅
- `util.rs:parse_coord` — 正确处理空/短坐标，返回清晰错误
- `run.rs:run` — 主类优先级逻辑正确（`[project].main` → `file_stem`）
- `diag.rs:threads` — 线程计数使用字符串匹配启发式（`starts_with('"') && contains('#')`），已知限制，可接受
- `search.rs:search/versions` — 使用 `parse_coord()` 共享实现，`serde_json` 反序列化正确
- `export.rs:maven` — pom.xml 生成逻辑正确

### 2. 安全性 ✅
- 无 SQL/XSS 注入风险
- 无敏感信息泄露
- 文件操作使用 `std::fs`，无路径穿越风险

### 3. 健壮性 ✅
- 所有错误使用 `Result<T>` + `?` 传播
- `run.rs:build_classpath` 中 `cs fetch` 失败时静默跳过（日志可加），不影响其他依赖
- `diag.rs:gc` 正确捕获 `jstat` stderr 并返回错误

### 4. 可维护性 — 发现 1 项 🟡
- `export.rs:maven` 中存在内联的坐标解析代码（`coord.split(':').collect()` + `parts[0]/parts[1]`），与 `util::parse_coord()` 逻辑完全相同但未引用共享实现

### 5. 性能 ✅
- `run.rs:build_classpath` 逐坐标调用 `cs fetch -p`，MVP 阶段可接受（批量获取需改接口）
- 无 N+1 查询、无不必要的循环

### 6. 规范 ✅
- `cargo fmt` 已统一格式
- 命名清晰一致
- 模块职责划分合理

## 问题列表

| # | 严重程度 | 文件 | 行号 | 描述 |
|---|----------|------|------|------|
| 1 | 🟢 | src/export.rs | 30-34 | 内联坐标解析应使用 `util::parse_coord()`（与步骤4精简 S1 同类问题，export.rs 未同步重构） |

## 汇总

- 🔴 必须修复：0
- 🟡 应该修复：0
- 🟢 可选改进：1
- **结论**：🟢 1 个可选改进（按严重度分级契约，🟢 同样登记入 Bugs 表并打回开发阶段）
