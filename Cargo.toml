[package]
name = "jex"
version = "0.2.0"
edition = "2021"
description = "uv/bun for Java —— 单二进制 JVM 工具链 CLI"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
