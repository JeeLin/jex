[package]
name = "jex-core"
version = "0.3.0"
edition = "2021"
description = "jex core library - JVM toolchain logic"
license = "MIT"

[dependencies]
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
