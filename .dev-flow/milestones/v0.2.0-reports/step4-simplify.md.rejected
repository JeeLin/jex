# 步骤4：代码精简报告

## 检查范围

`git diff --name-only dc26322` 过滤出 8 个源文件：

- `src/config.rs` (7 行)
- `src/deps.rs` (268 行)
- `src/diag.rs` (70 行)
- `src/error.rs` (42 行)
- `src/export.rs` (70 行)
- `src/jdk.rs` (232 行)
- `src/main.rs` (197 行)
- `src/run.rs` (106 行)
- `src/search.rs` (133 行)

## 检查维度

- 重复代码
- 死代码/未使用代码
- 过长函数
- 命名规范
- 代码组织

## 发现

### 🟡 S1: `parse_coord()` 在 deps.rs 和 search.rs 中重复定义

**位置**: `src/deps.rs:134-143` 和 `src/search.rs:100-109`
**问题**: 两个文件各有一份完全相同的 `parse_coord()` 函数（解析 `group:artifact` 坐标格式），共约 20 行重复代码
**影响**: 可维护性差——若坐标格式规则变化，需同步修改两处
**建议**: 提取到共享模块（如 `src/util.rs` 或 `src/error.rs`），两处引用统一版本

### 🟢 S2: search.rs `versions()` 函数标注 `#[allow(dead_code)]`

**位置**: `src/search.rs:98-99`
**问题**: `versions()` 是已实现的公共 API，但未在 `main.rs` 注册路由。`#[allow(dead_code)]` 掩盖了"实现但未接入"的状态
**影响**: 轻微——代码本身正确，仅注解掩盖了未完成的集成状态
**建议**: 在 main.rs 添加 Search 的 `--versions` 选项，或移除该函数

## 门禁判断

- 🟡 发现 1 项（S1：重复代码）
- 🟢 发现 1 项（S2：dead_code 注解）
- **结论：❌ 不通过** — 存在 🟡 发现，需登记到 Bugs 表格并打回开发阶段修复
