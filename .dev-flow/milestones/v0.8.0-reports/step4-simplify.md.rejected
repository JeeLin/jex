# 步骤4：代码精简 — v0.8.0 代码格式化

## 审查范围

通过 `git diff --name-only milestone-v0.8.0-start` 获取变更文件：
- `crates/jex-core/src/fmt.rs`（新建）
- `crates/jex-core/src/config.rs`（修改）
- `crates/jex-cli/src/main.rs`（修改）
- `crates/jex-core/src/lib.rs`（修改，模块声明）

## 发现

| # | 严重度 | 文件 | 问题描述 |
|---|--------|------|----------|
| 1 | 🟡 | `main.rs` | Fmt 处理器中 `if stdout / else if check / else write` 模式重复 3 次（changed files、single file、directory entries），应提取为 helper 函数 |
| 2 | 🟡 | `fmt.rs` | `format_file` 和 `format_code` 共享相同的 JAR 参数构建逻辑（查找 JAR、检查 java、构建 args），应提取为共享 helper |

## 结论

发现 2 个 🟡 级别问题，均属于可维护性缺陷（重复代码）。无 🔴 或 🟢 发现。

**结论**：🟡 打回开发阶段修复。
